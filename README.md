New file with dataset: daataset_custom.py, dataset_exploration.ipynb (it's just for data visualization)
Changed files: networks.py (changed the network), main_mnist.py (changed parameters and dataset)
